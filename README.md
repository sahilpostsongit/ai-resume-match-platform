# ai-resume-match-platform
# AI Resume Match Platform  An end-to-end mini-project that scores job seeker resumes against curated job postings using TF-IDF similarity. The stack includes a FastAPI backend with SQLite + SQLAlchemy, a lightweight ML layer, and a static frontend.
